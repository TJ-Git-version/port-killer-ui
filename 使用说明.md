# 端口占用关闭工具

查询并强制关闭占用指定端口的进程。包含两个独立版本：

| 版本 | 目录 / 文件 | 界面 | 启动方式 |
| --- | --- | --- | --- |
| tkinter 版 | `port_killer.py` | 简洁原生窗口 | 双击 `启动端口工具.bat` 或 `python port_killer.py` |
| Electron 版 | `port-killer-ui\` | 现代暗色主题、动画流畅 | 双击 `启动端口工具(新版).bat` 或 `cd port-killer-ui && npm start` |

两个版本功能一致，互不影响，可按喜好选用。

---

## tkinter 版（port_killer.py）

### 运行
- 双击 `启动端口工具.bat`（pythonw 无黑窗口），或命令行执行 `python port_killer.py`

### 使用步骤
1. 输入端口号（1~65535），回车或点「查询」
2. 列表显示占用进程：PID、进程名、协议、本地地址、状态
3. 点击第一列 ☐ 勾选，或「全选 / 取消全选」
4. 点「关闭选中进程」，确认后强制结束（`taskkill /PID /F`）
5. 权限不足时，点「工具 → 以管理员身份重启」提权

### 测试
```powershell
python -m unittest test_port_killer -v
```

---

## Electron 版（port-killer-ui）

### 运行
- 双击 `启动端口工具(新版).bat`，或：
  ```powershell
  cd D:\myCode\port\port-killer-ui
  npm start
  ```

### 首次使用前安装依赖
```powershell
cd D:\myCode\port\port-killer-ui
npm install
```
> 国内下载 Electron 较慢时可设置镜像：`$env:ELECTRON_MIRROR = "https://npmmirror.com/mirrors/electron/"; npm install`

### 功能亮点
- 暗色主题卡片式界面，悬停/弹窗/Toast/加载动画
- 常用端口快捷按钮、点击整行勾选、实时已选计数
- 自动忽略 PID=0 残留连接，权限不足一键提权重启
- 安全架构：系统命令只在主进程执行，页面无 Node 权限

### 测试
```powershell
cd D:\myCode\port\port-killer-ui
npm test                    # 核心逻辑单元测试
node scripts/verify.js      # 界面 DOM 验证（16 项）
node scripts/screenshot.js  # 界面截图（shots/ 目录）
```

---

## 通用注意事项

- 强制结束进程（/F）不会保存未保存的数据，请谨慎操作
- 系统关键进程（如 PID 4 System）请勿关闭
- 查询与关闭依赖 Windows 自带的 netstat / tasklist / taskkill

## 打包为 Windows / macOS / Linux 应用

新版（Electron）可打包成三个平台的应用，详见 `port-killer-ui/README.md`：

```powershell
cd D:\myCode\port\port-killer-ui
npm run build:win      # Windows 安装包 + 便携版（本机已验证）
npm run build:mac      # macOS（需在 macOS 上构建）
npm run build:linux    # Linux（需在 Linux 上构建）
```

已配置 GitHub Actions（`.github/workflows/build.yml`），推送到 GitHub 后自动在
Windows / macOS / Ubuntu 三个平台构建并产出安装包。
